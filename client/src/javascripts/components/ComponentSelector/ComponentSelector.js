import React, { Component } from 'react'
import axios from 'axios';

class ComponentSelector extends Component {
  state = {
  };

  componentDidMount() {
  };

  render() {
    return (
      <div>
        <h2>This will be the component selector</h2>
      </div>
    )
  }
}

export default ComponentSelector;
